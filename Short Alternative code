#include <stdio.h>
int main(){
    int n;
    printf("Enter the number.\n");
    scanf("%d", &n);
    int factorial = 1;
    for (int i = n; i >= 1; i--)
    {
        factorial = factorial * i;
    }
    printf("The factorial of %d is %d.\n", n, factorial);
    return 0;
    
}
